.MODEL SMALL
.STACK 100
.DATA
INPUT1 DB 'COLOR$'  
INPUT2 DB 'COLOUR$'
RESULT DB ?  ;STORED IN BL


.CODE   
MOV AX,@DATA 
MOV DS,AX
MOV ES,AX

CLD
MOV SI, OFFSET INPUT1 ; SI = INPUT1 OFFSET
MOV DI, OFFSET INPUT2 ; DI = INPUT2 OFFSET
MOV CX, 7 ; load the counter  
REPE CMPSB
JNE MISMATCH


MATCH: 
MOV BL,1 ;STORE 1 IF THE STRINGS ARE EQUAL
MOV RESULT,BL
JMP EXIT

MISMATCH:
DEC SI 
DEC DI  
MOV BL,0 ;STORE 0 IF THE STRINGS ARE EQUAL
MOV RESULT,BL

EXIT:
MOV AX,4C00H
INT 21H

END

     
